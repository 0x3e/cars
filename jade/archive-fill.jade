extends jade/layout
block content
  pre
    h2 Archive Fill
    :php
      if ( have_posts() ) {
        while ( have_posts() ) {
          the_post();
          the_title( '<h3>', '</h3>' );
          print_r(get_post_meta(get_the_ID()));
        }
      }
